import { configure } from '@testing-library/react';

/**
 * 非同期待ちの予算を1箇所で決める(#204)。
 *
 * **なぜ既定(1000ms)では足りないか。** `waitFor` / `findBy*` の待ち時間は**実時間**で数える。
 * 一方、待っている対象(React再レンダー・jsdomの描画・Dexieのフェイク)の所要時間は
 * **マシンの空き具合で伸びる**。つまり予算だけが固定で、仕事量の方が可変。
 *
 * **実測(2026-08-22・20コアのこのマシン)。** clientのテストを3本同時に走らせて比較した:
 *
 * | テスト | 単独 | 3本同時 | 倍率 |
 * |---|---|---|---|
 * | TermIndexScreen 段階描画(#135) | 318ms | **994ms** | 3.13x |
 * | TermIndexScreen「その他」 | 604ms | **1613ms** | 2.67x |
 * | App 登録単語数の表示 | 437ms | 885ms | 2.03x |
 * | SearchScreen Escape2段階 | 203ms | 553ms | 2.72x |
 *
 * 合計では1.63倍だが、**重いテストほど倍率が大きく最大3.1倍**になる。単独で300msを超える待ちは
 * 負荷時に1000msを越えうる——これが「実行のたびに違うファイルが落ちる」の正体で、
 * #186 で `TermIndexScreen` について実測した構造(描画の実コスト217msに対し予算が足りない)が
 * 他のファイルにも同じ形で存在していた。
 *
 * **負荷の出どころはテスト自身とは限らない。** 同じマシンで別のセッションがテストを回す、
 * CIのランナーを他のジョブと分け合う、といった外側の要因でも同じことが起きる
 * (実際 2026-08-22 に GitHub Actions 上でも `App.test.tsx` が1件落ちた)。
 * そのため vitest の並列度を下げても解決にはならない——**予算の側を仕事量に見合う値にする。**
 *
 * **これは sleep でもリトライでもない。** 条件が満たされ次第すぐ返るので速い場合の所要時間は変わらず、
 * 落ちたテストを再実行するわけでもない。代償は**本当に壊れている時に失敗が確定するまで最大5秒かかる**こと。
 */
configure({ asyncUtilTimeout: 5000 });
